func work(id, n) { sum := 0 i := 1 for i <= n { sum = sum + i i = i + 1 } println("worker", id, "computed sum 1..", n, "=", sum) }

func main() { println("spawning 5 goroutines") i := 1 for i <= 5 { go work(i, i * 1000) i = i + 1 } sleep(200) println("all goroutines finished") }

